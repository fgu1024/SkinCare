extends  ../layout

block content
    .container
        .row
            each item in products
                .col-md-2
                    .img-thumbnail
                        a(href='/product/' + item._id)
                            img(src=item.poster, alt=item.name)
                        .caption
                            h3 #{item.name}